version: '2'
services:
    reportingservice-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb reportingservice-mongodb-config
    reportingservice-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    reportingservice-mongodb-config:
        image: mongo:4.0.2
        container_name: reportingservice-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
